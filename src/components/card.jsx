const Card = (info) => {
    return (
        <div className="card">
            {console.log(info)}
            <IconGithub className="icon --adjust" />
            <IconLinkedIn className="icon" />
        </div>
    )
}

export default Card;